{
	"name": "@react-ui-lib/button-group",
	"description": "Button Group component",
	"version": "1.0.0",
	"license": "MIT",
	"author": "Audrius Navickas",
	"main": "dist/index.js",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "cross-env webpack --mode production --config ../../webpack.config.js",
		"clean": "rimraf dist && rimraf node_modules && rimraf package-lock.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@react-ui-lib/button": "^1.0.0"
	},
	"peerDependencies": {
		"react": "^16.8.2",
		"react-dom": "^16.8.2"
	}
}
