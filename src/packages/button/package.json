{
	"name": "@react-ui-lib/button",
	"description": "Button component",
	"version": "1.0.13-alpha.0",
	"license": "MIT",
	"author": "Prakash Naidu",
	"main": "dist/index.js",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "cross-env webpack --mode production --config ../../../webpack.config.js --env package=@react-ui-lib/button",
		"clean": "rimraf dist ",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"peerDependencies": {
		"react": "^17.0.2",
		"react-dom": "^17.0.2"
	}
}
