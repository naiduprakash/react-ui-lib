{
	"name": "@react-ui-lib/components",
	"description": "component library",
	"version": "1.0.0",
	"license": "MIT",
	"author": "Prakash Naidu",
	"main": "dist/index.js",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "cross-env webpack --mode production --config ../../../webpack.config.js --env package=@react-ui-lib/components",
		"clean": "rimraf dist ",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"peerDependencies": {
		"prop-types": "^15.8.1",
		"react": "^17.0.2",
		"react-dom": "^17.0.2"
	}
}
