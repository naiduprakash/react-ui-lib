{
	"name": "@react-ui-lib/alert",
	"description": "Alert component",
	"version": "1.0.5-alpha.0",
	"license": "MIT",
	"author": "Prakash Naidu",
	"main": "dist/index.js",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "cross-env webpack --mode development --config ../../webpack.config.js",
		"clean": "rimraf dist && rimraf node_modules && rimraf package-lock.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"peerDependencies": {
		"react": "^16.8.2",
		"react-dom": "^16.8.2"
	},
	"dependencies": {
		"@react-ui-lib/utils": "^1.0.8-alpha.0"
	}
}
