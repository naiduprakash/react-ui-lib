name: Build and Deploy
on: [push]
jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v2.3.1
            - name: Install and Build 🔧
            - run: git config user.email "prakash.naidu20101995@gmail.com"
            - run: git config user.name "naiduprakash"
            - run: git remote set-url origin https://naiduprakash:${{ secrets.TOKEN }}@github.com/naiduprakash/react-ui-lib.git
            - run: npm ci
            - run: npm run Install
            - run: npm run deploy
